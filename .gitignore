# ──────────────── Flutter / Dart ────────────────
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
.fvm/
**/generated_plugin_registrant.dart

# Build outputs
*.iml
*.ipa
*.apk
*.aab
*.app

# Coverage
coverage/
*.lcov

# Flutter web specific
# Sub-productos de `dart compile js` para el worker de Drift (ver web/drift_worker.dart).
# El bundle `drift_worker.dart.js` y `web/sqlite3.wasm` sí se traquean.
web/drift_worker.dart.js.deps
web/drift_worker.dart.js.map

# ──────────────── Plataformas ────────────────
android/app/debug
android/app/profile
android/app/release
android/.gradle/
android/captures/
android/local.properties
android/key.properties
*.jks

ios/Flutter/App.framework
ios/Flutter/Flutter.framework
ios/Flutter/Generated.xcconfig
ios/Flutter/app.flx
ios/Flutter/app.zip
ios/Flutter/flutter_assets/
ios/ServiceDefinitions.json
ios/Runner/GeneratedPluginRegistrant.*
ios/Pods/
ios/.symlinks/
ios/Flutter/flutter_export_environment.sh

linux/flutter/ephemeral/
macos/Flutter/ephemeral/
windows/flutter/ephemeral/
web/.dart_tool/

# ──────────────── Codegen ────────────────
# Los `*.g.dart` se versionan por ADR-0003/ADR-0004 (Riverpod y Drift).
*.freezed.dart
*.gr.dart
*.config.dart
*.mocks.dart

# ──────────────── Secretos y entornos ────────────────
.env
.env.local
.env.*.local
*.env
!.env.example

# Supabase local
supabase/.branches/
supabase/.temp/
supabase/.env

# ──────────────── IDE / Editor ────────────────
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ──────────────── Sistema operativo ────────────────
Desktop.ini
ehthumbs.db
ehthumbs_vista.db

# ──────────────── Flutter SDK local ────────────────
.flutter-sdk/

# ──────────────── Misc ────────────────
*.log
.cache/
tmp/
.tmp/

# ──────────────── Contratos para agentes IA (local-only) ────────────────
# Cómo le hablamos al modelo no es asunto del repo público. Viven en disco
# de cada dev pero no se traquean ni se publican.
AGENTS.md
CLAUDE.md
